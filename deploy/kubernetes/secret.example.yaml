# EXAMPLE ONLY: copy this file, replace every placeholder, and apply the copy.
# Never apply this example verbatim. See the deployment documentation for
# creating the database-owner role.
apiVersion: v1
kind: Secret
metadata:
  name: dufflebag
type: Opaque
stringData:
  database-url: postgres://dufflebag:<password>@db/dufflebag
  token-signing-key: <at least 32 random bytes>
